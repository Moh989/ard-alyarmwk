import{chromium,expect}from'@playwright/test';import fs from'node:fs/promises';
const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const c=await b.newContext({viewport:{width:1000,height:850}});const p=await c.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));p.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await p.goto('http://127.0.0.1:8088/pdf-viewer?lang=ar');await expect(p.locator('#pdf-canvas')).toHaveAttribute('data-rendered','1',{timeout:60000});for(let i=2;i<=10;i++){await p.locator('#pdf-next').click();await expect(p.locator('#pdf-canvas')).toHaveAttribute('data-rendered',String(i),{timeout:30000})}await expect(p.locator('#pdf-next')).toBeDisabled();await p.screenshot({path:'tests/screenshots/pdf-page10.png',fullPage:true});await p.locator('#pdf-page').fill('2');await p.locator('#pdf-page').press('Tab');await expect(p.locator('#pdf-canvas')).toHaveAttribute('data-rendered','2');await p.locator('#pdf-zoom').click();await expect(p.locator('#pdf-zoom')).toHaveAttribute('aria-pressed','true');await p.locator('.pdf-text summary').click();await expect(p.locator('#pdf-page-text')).not.toBeEmpty();await p.goto('http://127.0.0.1:8088/en/profile');await p.locator('iframe').scrollIntoViewIfNeeded();await expect(p.frameLocator('iframe').locator('#pdf-canvas')).toHaveAttribute('data-rendered','1',{timeout:60000});await p.screenshot({path:'tests/screenshots/pdf-embedded.png'});await fs.writeFile('tests/pdf-results.json',JSON.stringify({renderedPages:10,controls:true,embedded:true,errors},null,2));console.log({renderedPages:10,controls:true,embedded:true,errors});await b.close();
