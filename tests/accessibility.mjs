import {chromium} from '@playwright/test';import AxeBuilder from '@axe-core/playwright';import fs from'node:fs/promises';
const browser=await chromium.launch({executablePath:process.env.CHROME_PATH||'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const context=await browser.newContext({viewport:{width:1440,height:1000},reducedMotion:'reduce'});const page=await context.newPage();const result=[];
for(const lang of ['ar','en'])for(const route of ['','about','sectors','sectors/energy','profile','contact','privacy','missing']){await page.goto(`http://127.0.0.1:8088/${lang}/${route}`);await page.evaluate(()=>document.fonts.ready);const axe=await new AxeBuilder({page}).withTags(['wcag2a','wcag2aa','wcag21aa']).analyze();result.push({path:`/${lang}/${route}`,violations:axe.violations.map(v=>({id:v.id,impact:v.impact,description:v.description,nodes:v.nodes.map(n=>({target:n.target,summary:n.failureSummary}))}))})}
await fs.writeFile('tests/accessibility-results.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result.filter(x=>x.violations.length),null,2));await browser.close();
